<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="PO04_2P_GUERRERO_MARQUEZ_PALAGUACHI"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POO4_2P_GUERRERO_MARQUEZ_PALAGUACHI" >
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuParticipanteActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".MenuAdministradorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Tabla de Posiciones -->
        <activity
            android:name=".TablaPosiciones"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Menú Participante -->
        <activity
            android:name=".MenuParticipanteActivity"
            android:exported="false" />

        <!-- Menú Administrador -->
        <activity
            android:name=".MenuAdministradorActivity"
            android:exported="false" />
    </application>

</manifest>